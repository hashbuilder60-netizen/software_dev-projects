name: Algorithms Toolkit CI

on:
  push:
    branches: [ main ]
    paths:
      - 'python-algorithms-toolkit/**'
      - '.github/workflows/python-algorithms-toolkit-ci.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'python-algorithms-toolkit/**'
      - '.github/workflows/python-algorithms-toolkit-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python-algorithms-toolkit
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: |
          python -m pip install --upgrade pip
          pip install pytest
      - run: pytest -q