name: FastAPI Task API CI

on:
  push:
    branches: [ main ]
    paths:
      - 'fastapi-task-api/**'
      - '.github/workflows/fastapi-task-api-ci.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'fastapi-task-api/**'
      - '.github/workflows/fastapi-task-api-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fastapi-task-api
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]
      - name: Run tests
        run: pytest -q