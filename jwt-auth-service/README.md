# JWT Auth Service

TypeScript auth service demonstrating register/login/protected route with JWT.

## Run

```powershell
cd jwt-auth-service
npm install
npm run dev
```

## Endpoints
- `POST /register`
- `POST /login`
- `GET /me` (Bearer token required)